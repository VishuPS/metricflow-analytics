name: Actions Smoke Test

on:
  workflow_dispatch:

jobs:
  smoke:
    name: Confirm Actions can queue
    runs-on: ubuntu-latest
    steps:
      - name: Print confirmation
        run: echo "GitHub Actions queued and ran successfully."
